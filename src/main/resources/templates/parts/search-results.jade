if cards
    .ui.main.fluid.vertical.segment
        table.ui.celled.table
            thead
                tr
                    th Name
                    th.center.aligned Mana cost
                    th Type
                    th Tags
                    th.center.aligned Set
                    th
            tbody
                for card in cards
                    tr
                        td.collapsing #{card.name}
                        td.collapsing #{card.manaCost}
                        td.collapsing #{card.type}
                        td
                            if card.tags
                                .ui.blue.labels
                                    for tag in card.tags
                                        .ui.label #{tag}
                        td.collapsing #{card.set}                                        
                        td.collapsing
                            button.ui.primary.button(
                                hx-get="/api/cards/#{card.multiverseId}",
                                hx-target="#card"
                                hx-trigger="click"
                                ) View details
                                i.angle.double.right.icon
            tfoot
                tr
                    th(colspan="6")
                        .ui.right.floated.metadata.menu
                            if metadata.pagination
                                a.icon.item(
                                    hx-get!=metadata.pagination.first.href
                                    hx-target="#search-results"
                                    )
                                    i.step.backward.icon
                                for link in metadata.pagination.pages
                                    a.item(
                                        hx-get!=link.href
                                        hx-target="#search-results"
                                        class=link.currentPage ? "active" : ""
                                    ) #{link.name}
                                a.icon.item(
                                    hx-get!=metadata.pagination.last.href
                                    hx-target="#search-results"
                                    ) 
                                    i.step.forward.icon
else
    .ui.main.fluid.vertical.segment
        .ui.blue.message No results