if cards
    .ui.main.fluid.vertical.segment
        table.ui.very.basic.single.line.table
            tbody
                for card in cards
                    tr
                        td.collapsing #{card.name}
                        td
                           if card.tags
                               .ui.violet.labels
                                   for tag in card.tags
                                       .ui.label #{tag}
                        td.collapsing
                            .ui.primary.button(
                                ic-get-from="/api/cards/#{card.multiverseId}",
                                ic-target="#card"
                                ) View details
                                i.angle.double.right.icon

else
    .ui.main.fluid.vertical.segment
        .ui.violet.message No results